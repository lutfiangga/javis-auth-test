# frontend/src/assets

Folder aset yang diimport dari source code.

## Isi
- `hero.png` -> ilustrasi hero.
- `react.svg` -> asset logo React.
- `vite.svg` -> asset logo Vite.
- `svg/logo.tsx` -> komponen logo custom berbasis SVG.

## Catatan
- Gunakan folder ini untuk aset yang perlu diproses bundler.

