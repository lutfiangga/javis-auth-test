# frontend/src/components

Folder semua komponen React aplikasi.

## Subfolder
- `auth` -> route guard untuk halaman public/protected.
- `layout` -> layout wrapper halaman.
- `partials` -> komponen domain aplikasi (auth form, navbar, card, user list).
- `ui` -> komponen dasar reusable (button, input, table, badge, dst).

